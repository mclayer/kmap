<svg:g
  [style.transform] = "'translate('+ node.x +'px, '+ node.y +'px)'"
  (click)="editTitle(node)"
>
  <svg:rect
    width="300"
    height="50"
    stroke="green"
    stroke-width="4"
    fill="yellow"
  />
  <svg:text dx="10" dy="10">{{node.title}}</svg:text>
</svg:g>
